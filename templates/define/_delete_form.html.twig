<form method="post" action="{{ path('app_define_delete', {'id': define.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ define.id) }}">
    <button class="btn">Delete</button>
</form>
